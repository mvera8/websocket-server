version: "3.4"

services:
  backend:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000

  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile.frontend
    ports:
      - 5001:5000
